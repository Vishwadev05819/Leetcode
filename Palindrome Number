class Solution {
    public boolean isPalindrome(int x) 
    {
        int rem,sum=0;
        int n=x;
        while(n>0)
        {
            rem=n%10;
            sum=(sum*10)+rem;
            n=n/10;
        }
        return sum==x;
    }
}
